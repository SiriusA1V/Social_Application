@charset 'UTF-8';

@import '../../settings.pcss';

.p-event-top {
    &__contents {
        padding-bottom: 20px; 
        &__reservation-event,
        &__group-event,
        &__event-list {
            &__title {
                padding: 30px 10px 16px;
            }
        }
    }
}
